import {} from "../types";

const initialState = {
  pokemonBox: [] 
}

const reducer = (state = initialState, action) => {
  switch(action.type){
    default:
      return state
  }
}

export default reducer;